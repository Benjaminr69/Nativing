<% provide(:title, 'Profil') %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1><%= @user.fname %> <%= @user.lname %></h1>
		<h2><%= @user.email %></h2>
		<%= link_to image_tag(@user.photo.url(:thumb)), @user.photo.url %><br>
		<%= link_to "Editer le profil", edit_user_path(current_user) %>
    </section>
  </aside>
</div>